class Agenda {
    constructor(id, dataHora, idPaciente, idProfissional) {
      this.id = id;
      this.dataHora = dataHora;
      this.idPaciente = idPaciente;
      this.idProfissional = idProfissional;
    }
  }
  
  module.exports = Agenda;
  